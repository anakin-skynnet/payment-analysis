# Minimal requirements for Databricks Apps (maximize compatibility).
# Uses pre-installed fastapi 0.115.0 and uvicorn 0.30.6; only adds/overrides what's needed.
# If "error installing packages" persists with requirements.txt, try:
#   cp requirements-databricks-minimal.txt requirements.txt
# then redeploy. See docs/DEPLOYMENT_GUIDE.md#databricks-apps-compatibility.

# Override pre-installed (we need a newer SDK)
databricks-sdk==0.84.0

# Not pre-installed (app needs these)
pydantic-settings==2.6.1
sqlmodel==0.0.27
psycopg==3.2.3
