# Resolve npm ERESOLVE peer dependency conflicts (e.g. @tanstack/react-router family).
# Databricks App build runs npm install; overrides in package.json align versions;
# this fallback ensures install succeeds if the platform's npm still reports conflicts.
legacy-peer-deps=true
