# Databricks App: only packages NOT pre-installed (fastapi, uvicorn, databricks-sdk are pre-installed).
# Pinned versions to avoid "error installing packages" (no source builds; pure Python psycopg).
pydantic-settings==2.6.1
sqlmodel==0.0.27
psycopg==3.2.3
