# Direct dependencies for Databricks App runtime.
# Transitive deps resolved by pip; only pin direct deps to avoid conflicts.
# NOTE: Do NOT include package.json in the deployed workspace — it triggers
# npm install which fails. The UI is pre-built into __dist__/.
databricks-sdk==0.86.0
fastapi==0.128.7
uvicorn==0.40.0
pydantic-settings==2.12.0
sqlmodel==0.0.32
psycopg[binary,pool]==3.3.2
